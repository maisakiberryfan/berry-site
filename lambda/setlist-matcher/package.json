{
  "name": "berry-setlist-matcher",
  "version": "1.0.0",
  "description": "AWS Lambda function for setlist fuzzy matching",
  "type": "module",
  "main": "src/handler.js",
  "scripts": {
    "build": "sam build",
    "deploy": "sam build && sam deploy",
    "local": "node scripts/start-local.js",
    "local:simple": "sam local start-api -n env.json"
  },
  "dependencies": {
    "fast-fuzzy": "^1.12.0",
    "leven": "^4.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}